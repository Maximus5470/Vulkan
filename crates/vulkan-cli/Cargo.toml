[package]
name = "vulkan-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
vulkan-core = { path = "../vulkan-core" }
vulkan-server = { path = "../vulkan-server" }
dotenvy = "0.15"
redis = "1"